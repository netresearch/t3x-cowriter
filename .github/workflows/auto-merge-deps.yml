name: Auto-merge dependency PRs
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
permissions: {}
jobs:
  auto-merge:
    uses: netresearch/typo3-ci-workflows/.github/workflows/auto-merge-deps.yml@273cb8fd37b60ae31f72834d9896007623c99bf4 # main
    permissions:
      contents: write
      pull-requests: write
