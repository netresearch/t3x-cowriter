name: CodeQL
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'
permissions: {}
jobs:
  analyze:
    uses: netresearch/typo3-ci-workflows/.github/workflows/codeql.yml@d2aa25fc2bdb143c88f24cadfffe444c42908409 # main
    permissions:
      contents: read
      security-events: write
      actions: read
